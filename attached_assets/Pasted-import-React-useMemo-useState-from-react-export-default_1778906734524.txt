import React, { useMemo, useState } from 'react'

export default function VelozDriverApp() { const weeklyData = [40, 65, 55, 85, 70, 100, 90]

const [earnings, setEarnings] = useState(482) const [fuel, setFuel] = useState(98) const [rides, setRides] = useState(23)

const netProfit = useMemo(() => { return earnings - fuel }, [earnings, fuel])

return ( <div className="min-h-screen bg-black text-white font-sans"> <div className="max-w-md mx-auto p-4 space-y-5">

{/* Premium Header */}
    <div className="relative overflow-hidden bg-zinc-900 border border-yellow-400/20 rounded-[32px] p-6 shadow-2xl">
      <div className="absolute top-0 right-0 w-40 h-40 bg-yellow-400/10 blur-3xl rounded-full"></div>

      <div className="flex items-center justify-between relative z-10">
        <div>
          <p className="text-zinc-500 text-sm">Boa noite, Thyago</p>
          <h1 className="text-3xl font-black tracking-wide mt-1">
            VELOZ <span className="text-yellow-400">DRIVER</span>
          </h1>
        </div>

        <div className="w-14 h-14 rounded-3xl bg-yellow-400 text-black flex items-center justify-center font-black text-2xl shadow-lg shadow-yellow-400/30">
          V
        </div>
      </div>
    </div>

    {/* Main Profit Card */}
    <div className="relative overflow-hidden bg-gradient-to-br from-yellow-400 via-yellow-300 to-amber-500 rounded-[36px] p-7 text-black shadow-2xl">
      <div className="absolute -right-10 -top-10 w-40 h-40 bg-white/20 rounded-full blur-2xl"></div>

      <div className="relative z-10">
        <p className="font-semibold opacity-70">Lucro Líquido Hoje</p>
        <h2 className="text-6xl font-black mt-2 tracking-tight">R$ {netProfit}</h2>

        <div className="mt-6 flex items-center justify-between">
          <div>
            <p className="text-sm opacity-70">Meta diária</p>
            <p className="font-bold text-lg">R$ 600</p>
          </div>

          <div className="bg-black/10 px-4 py-2 rounded-2xl backdrop-blur-sm">
            <p className="font-black text-2xl">80%</p>
          </div>
        </div>

        <div className="w-full bg-black/10 rounded-full h-4 mt-4 overflow-hidden">
          <div className="bg-black h-4 rounded-full w-4/5"></div>
        </div>
      </div>
    </div>

    {/* Quick Stats */}
    <div className="grid grid-cols-2 gap-4">
      {[
        ['Horas Online', '8.4h'],
        ['KM Rodados', '164'],
        ['Combustível', `R$ ${fuel}`],
        ['Corridas', `${rides}`]
      ].map(([title, value], index) => (
        <div
          key={index}
          className="bg-zinc-900 border border-zinc-800 rounded-[28px] p-5 shadow-xl"
        >
          <p className="text-zinc-500 text-sm">{title}</p>
          <h3 className="text-3xl font-black text-yellow-400 mt-3">
            {value}
          </h3>
        </div>
      ))}
    </div>

    {{/* Quick Actions */}
    <div className="bg-zinc-900 border border-zinc-800 rounded-[32px] p-5 shadow-xl">
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-xl font-black">Ações Rápidas</h3>
        <div className="text-yellow-400 text-2xl">💸</div>
      </div>

      <div className="grid grid-cols-2 gap-3">
        <button
          onClick={() => setEarnings(earnings + 50)}
          className="bg-yellow-400 text-black font-bold rounded-2xl py-4 transition-all active:scale-95"
        >
          + R$50
        </button>

        <button
          onClick={() => setRides(rides + 1)}
          className="bg-zinc-800 text-white font-bold rounded-2xl py-4 transition-all active:scale-95"
        >
          + Corrida
        </button>
      </div>
    </div>

    {/* Weekly Chart */}}
    <div className="bg-zinc-900 border border-zinc-800 rounded-[32px] p-5 shadow-2xl">
      <div className="flex items-center justify-between mb-5">
        <div>
          <h3 className="text-xl font-black">Ganhos da Semana</h3>
          <p className="text-zinc-500 text-sm mt-1">
            Seu melhor resultado nos últimos 30 dias
          </p>
        </div>

        <div className="bg-yellow-400/10 text-yellow-400 px-4 py-2 rounded-2xl font-bold">
          +18%
        </div>
      </div>

      <div className="h-44 flex items-end gap-2">
        {weeklyData.map((height, index) => (
          <div key={index} className="flex-1 flex flex-col justify-end items-center gap-2">
            <div
              className="w-full bg-gradient-to-t from-yellow-500 to-yellow-300 rounded-t-3xl transition-all duration-700"
              style={{ height: `${height}%` }}
            />
          </div>
        ))}
      </div>

      <div className="flex justify-between mt-4 text-xs text-zinc-500 font-semibold">
        <span>SEG</span>
        <span>TER</span>
        <span>QUA</span>
        <span>QUI</span>
        <span>SEX</span>
        <span>SAB</span>
        <span>DOM</span>
      </div>
    </div>

    {/* Financial AI */}
    <div className="bg-zinc-900 border border-zinc-800 rounded-[32px] p-5 shadow-xl">
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-xl font-black">Resumo Inteligente</h3>
        <div className="text-yellow-400 text-2xl">⚡</div>
      </div>

      <div className="space-y-3">
        <div className="bg-black/30 rounded-2xl p-4 flex justify-between">
          <span className="text-zinc-400">Média por hora</span>
          <span className="font-bold text-yellow-400">R$ 57/h</span>
        </div>

        <div className="bg-black/30 rounded-2xl p-4 flex justify-between">
          <span className="text-zinc-400">Melhor horário</span>
          <span className="font-bold text-yellow-400">18h - 22h</span>
        </div>

        <div className="bg-black/30 rounded-2xl p-4 flex justify-between">
          <span className="text-zinc-400">Gasto semanal</span>
          <span className="font-bold text-yellow-400">R$ 682</span>
        </div>
      </div>
    </div>

    {/* Navigation */}
    <div className="h-32"></div>
  </div>

  {/* Bottom Premium Nav */}
  <div className="fixed bottom-4 left-1/2 -translate-x-1/2 w-[94%] max-w-md bg-zinc-900/95 backdrop-blur-xl border border-zinc-800 rounded-[32px] p-4 shadow-2xl">
    <div className="flex justify-around">
      {[
        ['🏠', 'Home', true],
        ['💰', 'Ganhos'],
        ['⛽', 'Gastos'],
        ['📊', 'Relatórios'],
        ['⚙️', 'Config']
      ].map(([icon, label, active], index) => (
        <div
          key={index}
          className={`flex flex-col items-center gap-1 ${active ? 'text-yellow-400' : 'text-zinc-500'}`}
        >
          <div
            className={`w-12 h-12 rounded-2xl flex items-center justify-center text-lg ${
              active
                ? 'bg-yellow-400/20 shadow-lg shadow-yellow-400/10'
                : 'bg-zinc-800'
            }`}
          >
            {icon}
          </div>

          <span className="text-xs font-semibold">{label}</span>
        </div>
      ))}
    </div>
  </div>
</div>

) }